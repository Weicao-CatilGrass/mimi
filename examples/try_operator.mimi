// Test ? operator with user-defined Result-like types

type Res {
    Ok(i32)
    Err(string)
}

func safe_div(a: i32, b: i32) -> Res {
    if b == 0 {
        return Err("division by zero");
    }
    return Ok(a / b);
}

func main() -> i32 {
    // Test with ? - success path
    let result = safe_div(10, 2)?;
    println(result);
    result
}
