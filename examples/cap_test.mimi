// Test cap (linear capability) syntax
// Note: caps are declared types; this tests the declaration syntax

cap FileReadCap;

func main() -> i32 {
    // Cap declarations are types - just verify the type checks
    println(42);
    42
}
